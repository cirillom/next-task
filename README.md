# Next Task

Next Task is a small, self-hosted task manager that ranks unfinished work using a
workspace-configurable score. The FastAPI backend, SQLite database, and Svelte PWA
ship as one application container.

Implementation is in progress.

